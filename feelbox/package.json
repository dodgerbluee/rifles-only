{
  "name": "rifles-feelbox",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "dev:full": "node server/index.mjs --dev",
    "start": "node server/index.mjs --dev",
    "server": "node server/index.mjs",
    "build": "tsc --noEmit && vite build",
    "docker": "docker compose up --build"
  },
  "dependencies": {
    "three": "^0.170.0",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
